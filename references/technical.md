# Technical
Review HTTP status, redirects, canonical/indexability consistency, robots meta, viewport,
language, sitemap/robots references, structured-data parseability, internal link integrity,
rendering risks and performance evidence. Never auto-change noindex, canonical, redirects,
hreflang or robots rules without human review. Core Web Vitals require field/lab data; do not
infer them from HTML alone.