# Contributing
Keep the core zero-dependency unless there is a compelling reason otherwise. Prefer focused
references/playbooks over adding runtime packages. New deterministic checks should document
applicability, evidence, false positives, correction and validation, and include a regression test.
